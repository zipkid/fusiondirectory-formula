# fusiondirectory-formula

Requires the Apache formula from https://github.com/saltstack-formulas/apache-formula.git


Pillar config

```yaml
fusiondirectory:
  config:
    admin_user: admin
    admin_pwd: <pwd>
    pwd_salt: <salt>
```
